# Fixtures

`good-pack/` is a FICTIONAL tiny Unity project plus a workflow pack that must validate with 0 errors and 0 warnings.
It exists only to test the tools. Do not read anything under `tests/` while generating or refreshing a real pack,
and never copy its names, paths, or claims into a real project's pack.
