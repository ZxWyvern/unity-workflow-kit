# AI workflow pack (fixture)

Profile: standard. Files: agent.md, project-context.md, development-workflow.md, validation-matrix.md, session-handoff.md, index.json.
`index.json` is a navigation index, not executable configuration.
Pack validation (executed) is reported separately from game checks (none run).
